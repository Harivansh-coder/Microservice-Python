apiVersion: v1
kind: Service
metadata:
  name: auth

spec:
  selector:
    app: auth
  ports:
    - protocol: TCP
      port:
        5000
        # The port that the service will expose
      targetPort: 5000
  type: ClusterIP
